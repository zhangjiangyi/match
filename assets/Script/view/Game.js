cc.Class({
    extends: cc.Component,

    properties: {
        actionNode: cc.Node,
        scoreLabel: cc.Label,
        countDownLabel: cc.Label,
    },

    start () {

    },

    onBtnClick(event, eventName) {
        switch (eventName) {
            case 'Up':
                break;
            case 'Down':
                break;
            case 'Left':
                break;
            case 'Right':
                break;

        }
    }
});
